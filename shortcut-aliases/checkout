[alias]
  ch = checkout
  chb = checkout -b
  chd = checkout dev
  chf = checkout --force
  chh = checkout -h
  chm = checkout master
  cho = checkout --orphan
  chq = checkout --quiet
  cht = checkout --track
  chv = checkout --verbose