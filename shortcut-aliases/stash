[alias]
  st = stash
  sta = stash apply
  stb = stash branch
  stc = stash clear
  std = stash drop
  sth = stash -h
  stp = stash pop
  stl = stash list
  sts = stash save
  stski = stash save --keep-index
  stsnki = stash save --no-keep-index
  stsu = stash save --include-untracked